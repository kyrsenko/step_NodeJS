<div class="card">
     <button type="button" class="close todo-delete-btn" aria-label="Close" data-id="<%= note._id%>">x</button>
    <div class="card-body">
      <h5 class="card-title"name="note-title"><%= note.title %></h5>
      <p class="card-text" name="note-text"><%= note.text %></p>
      <button type="button" data-id="<%= note._id%>" class="btn btn-primary edit-note-btn">Edit</a>
    </div>
  </div>