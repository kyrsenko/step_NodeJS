<h2 class="fixed-top text-center text-white"><%= pageTitle%></h2>
<div class="row">
  <div class="col-md-10 rounded border border-dark  bg-white d-flex flex-column p-2 mx-auto mb-4">
    <div class="card border-0">
        <button type="button" class="close note-delete-btn align-self-end field-delete d-block p-2 position-absolute mt-1" aria-label="Close" data-id="<%= note._id%>"></button>
        <div class="card-body input-wrapper">
          <h5 class="card-title text-center"name="note-title"><%= note.title %></h5>
          <p class="card-text" name="note-text"><%= note.text %></p>
          <button type="button" data-id="<%= note._id%>" class="btn btn-warning edit-note-btn d-block m-auto align-self-center">Edit</a>
        </div>
      </div>
    </div>
  </div>