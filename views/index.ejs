
    <div class="row">
<div class="col-12 mx-auto">

    <h2 class="text-center">TODO</h2>
        <!-- <div class="row"> -->
        <% lists.forEach(item =>{%>
            <div class="col-12 rounded border border-dark  bg-white d-flex flex-column  py-2 mb-1 mx-auto">
                    <!-- <button type="button" class="delete-list-btn btn align-self-end field-delete d-block p-2" data-id="<%= item._id%>"></button> -->
                    <%if(item.title){%>
                    <h3 class="card-title text-truncate text-center"><%= item.title%></h3>
                    <%}%>
            <ol class="list-group list-group-flush px-4 py-2">
                <%item.text.forEach(item => {%>
                    <li class="list-group-item border-0 px-2 py-0 text-break text-truncate dot <%= item.status%>"><%= item.text%></li>
                <%})%>
            </ol>
            <button type="button" class="view-list-btn btn btn-secondary d-block mt-auto mb-3 w-25 align-self-center" data-id="<%= item._id%>">view</button>
        </div>
            <%})%>
        </div>
        <!-- </div> -->
    </div>
   <!-- </div> -->

<div class="row">
    <div class="col-12 mx-auto">
        <h2 class="text-center">Notes</h2>
    <% notes.forEach(item => {%>
    <div class="card my-1 border border-dark">
        <div class="card-body">
            <% if(item.title){%>
            <h5 class="card-title text-center"><%= item.title%></h5>
            <%}%>
                <p class="card-text text-truncate"><%= item.text%></p>
            <a href="#" class="btn btn-secondary d-block note-view-btn w-25 mx-auto mt-auto" data-id="<%= item._id%>">View</a>
        </div>
    </div>
    <% })%>
</div>
</div>

